# 卡牌辉光效果扩展计划

> 状态：已立项，待排期实现
> 背景：辉光（aura）当前只有一种默认效果——暖橙渐变、紧致描边 + 柔和外光。
> 目标：提供多种辉光样式，丰富卡牌装饰。所有辉光必须遵守项目硬约束：
> **禁止时间动画（无呼吸/脉冲频闪），光效仅由视角（拖动旋转）驱动或静态呈现。**

## 一、辉光参数体系（底层改造）

在 webcard.py 中新增 `GLOW_EFFECTS` 预设表（仿照 `SEAL_EFFECTS` 结构）：

| 参数 | 说明 | 示例值 |
|---|---|---|
| `colorTop` / `colorBottom` | 辉光颜色渐变两端（或单色） | 暖橙 `#fca01f`→`#6e1406` |
| `tight` | 紧致描边强度（边缘 0~0.014 卡高） | 0.14 |
| `soft` | 柔和外光强度（0.006~0.06 卡高） | 0.045 |
| `mode` | 形状/分布模式（见下） | uniform / corners / edge_gradient |
| `fresnel` | 是否叠加随视角倾斜增强的菲涅尔光（视角驱动，无频闪） | 0 / 0.3 |

## 二、辉光样式清单（首批候选）

1. **暖金描边**（默认，现状）—— 深红→金黄渐变，紧致+柔光
2. **冰蓝冷光** —— 深蓝→冰蓝渐变，柔和为主（soft 提升、tight 降低）
3. **紫罗兰夜光** —— 紫→粉渐变
4. **四角聚焦** —— 四角辉光更亮、边中点较弱（corner 模式）
5. **上下双色** —— 顶部冷色 + 底部暖色沿轮廓渐变（edge_gradient）
6. **菲涅尔描边** —— 平视时辉光弱、倾斜拖动时边缘光随视角增强（视角驱动）
7. **双环辉光** —— 紧致高亮环 + 外围大范围淡光（两层 smoothstep 独立调节）

## 三、界面与存储

- 拼装页 + 生成页各新增「辉光」下拉框（选项 = GLOW_EFFECTS 键 + 「无」）与强度输入（默认 1.0，范围 0~2）
- 保存到卡 JSON：`glow_name` / `glow_strength`，加载成品卡时回填
- CLI 补 `--glow` / `--glow-strength` 参数
- aura shader 按预设注入 uniform（uGlowColorTop/Bottom、uGlowTight/uGlowSoft、uGlowMode 等）
- 无边框时回退默认辉光逻辑保持不变；有边框时按蒙版 SDF 绘制，样式参数同样生效

## 四、2D 合成同步（可选后续）

compositor.py 的 2D 成品图暂无辉光；如需 2D 预览一致，需按蒙版边缘静态绘制辉光
（无视角驱动，仅静态渲染），优先级低于 3D。

## 五、验收

- 每种辉光样式：无边框（沿卡体圆角矩形）与有边框（沿蒙版轮廓）下均正确显示
- 无任何时间动画/频闪；拖动旋转时样式正常、仅视角光效变化
- 旧卡加载：未填 glow_name 时保持默认辉光，不回退为「无」
