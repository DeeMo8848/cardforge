# 测试流程 · 辉光特效版本（v7b9f030）

> 覆盖本次新增的辉光/边框特效、素材管理两个新分类，以及旧功能回归。
> 测试方式：手动操作 Web 界面（http://127.0.0.1:8765）+ 少量命令行。

## 0. 测试前准备

1. 启动服务：`venv\Scripts\python.exe webapp.py --no-browser`（若已在跑可跳过）
2. 浏览器打开 http://127.0.0.1:8765 ，**强制刷新 Ctrl+F5**（必须，否则用的还是旧前端）
3. 准备一张测试图（可用素材库里已有的卡面/背景，或任意一张 PNG）

---

## 1. 新功能：辉光 / 边框特效

### 1.1 拼装页（重点测试区）

| # | 步骤 | 预期 |
|---|---|---|
| 1 | 进「卡牌拼装」页，看「辉光效果」下拉 | 共 12 项：不使用 + 11 种辉光 |
| 2 | 逐项切换**静态类**：暖金描边 / 冰蓝冷光 / 紫罗兰夜光 / 四角聚焦 / 上下双色 / 菲涅尔描边 / 双环辉光 | 3D 预览实时刷新；菲涅尔描边在**拖动旋转卡片**时边缘光变化 |
| 3 | 逐项切换**动画类**：RGB变色灯光 / 霓虹灯 / 发廊螺纹 | 等待 2~3 秒观察：RGB 边缘颜色循环（红→绿→蓝）；霓虹红/蓝交替；发廊螺纹旋转彩条 |
| 4 | 调「辉光强度」0 → 0.5 → 2 | 0=无辉光；2=明显增强 |
| 5 | 选「不使用」 | 无辉光，仅卡体/边框 |
| 6 | 选好辉光后「保存为卡牌」→ 重新加载该卡 | 辉光名与强度**回填**到下拉/输入框 |
| 7 | 点「重置」 | 辉光恢复默认（暖金描边），强度 1 |

### 1.2 生成页

| # | 步骤 | 预期 |
|---|---|---|
| 1 | 进「生成卡牌」页，辉光下拉同样 12 项 | 正常显示 |
| 2 | 选「樱花粉」+ 强度 1.3，上传图生成卡牌 | 结果卡带粉色辉光；「在拼装页打开」后拼装页辉光下拉是樱花粉 |

### 1.3 命令行（可选）

```bat
cardforge.cmd 封面.png --name "测试辉光" --glow 樱花粉 --glow-strength 1.3
cardforge.cmd 封面.png --name "测试霓虹" --glow 霓虹灯
:: none=关闭辉光
cardforge.cmd 封面.png --name "无辉光" --glow none
```

预期：生成卡牌的 3D 网页（assets/output/<slug>/card.html）带对应辉光。

---

## 2. 新功能：素材管理（特效 / 边框特效）

> 入口：主页面 →「素材管理」→ 右下角分类。

### 2.1 🎆 特效

| # | 步骤 | 预期 |
|---|---|---|
| 1 | 点「🎆 特效」分类 | 列出 8 个内置（标注「内置」）+ 2 个自定义（彩球动画、樱花雨，无标注） |
| 2 | 删除自定义「樱花雨」 | 弹确认 → 删除后列表消失；拼装页/生成页特效选择器里同步消失 |
| 3 | 删除内置「coin」（或其他不常用的） | 弹确认（提示同步删源码定义）→ 删除后列表消失；**重启服务后仍在消失状态**（验证源码定义真的被删了） |

⚠️ **内置特效删除不可恢复**（除非从 GitHub 重新拉代码），测试时只删不常用的。

### 2.2 💡 边框特效

| # | 步骤 | 预期 |
|---|---|---|
| 1 | 点「💡 边框特效」分类 | 列出 11 个内置辉光（标注「内置」） |
| 2 | 删除内置「双环辉光」 | 弹确认 → 删除后列表消失；辉光下拉里同步消失；重启后仍消失 |

### 2.3 恢复方法（测试后还原）

```bat
git checkout -- webcard.py cardforge.py
:: 或直接 git pull 拉回最新
```

---

## 3. 回归测试（确认旧功能没被弄坏）

| # | 功能 | 步骤 | 预期 |
|---|---|---|---|
| 1 | 卡封 | 拼装页选卡封（如某张镭射膜） | 3D 卡有膜光效，翻到卡背显示正常（无黑块） |
| 2 | 边框卡封 | 选边框 + 边框卡封「与卡封一致」 | 卡背统一显示边框卡封，正面卡封正常 |
| 3 | 裁剪到边框内 | 勾选/取消该复选框 | 勾选=主体裁在边框内；取消=主体可延伸出边框；保存后重开保持 |
| 4 | 主体浮于边框之上 / 主体描边 | 勾选两个开关 | 3D 预览立体感/白边正常 |
| 5 | 缩放 | 填 0.5 / 1.5 | 预览实时变化，保存后回填 |
| 6 | 粒子特效 | 特效选择器加 love / sparkle | 3D 卡有冒爱心/闪光动画 |
| 7 | 导出 | 保存为图片 / 保存为网页 | 图片透明 PNG 正常；网页为自包含单文件，双击可打开 |
| 8 | 收集册 | 打开收集册 → 点卡牌 | 3D 预览弹出，导出按钮可用，删除按钮在 2D 预览左上 |
| 9 | 抠图工具 | 打开 /cutout.html 传图 | 抠图正常出透明 PNG |
| 10 | 文本型卡牌 | 切换文本型，调文本区「上」滑块 | 卡面随文本区移动，缩放保持 |

---

## 4. 已知说明

- **动画类辉光**：霓虹灯带轻微灯管频闪是设计效果；RGB/发廊螺纹为平滑循环。
- **旧卡**：辉光功能上线前生成的卡，加载时默认回退「暖金描边」，重新保存一次即固定。
- **改了源码后**：重启服务 + 强刷浏览器（Ctrl+F5）才生效。
- 卡牌 3D 预览的**动画/光效由拖动旋转驱动**，静止时部分效果（菲涅尔描边）不明显属正常。

---

## 5. 测试记录表

| # | 用例 | 通过 | 备注 |
|---|---|---|---|
| 1.1-1 | 拼装页辉光下拉 12 项 | ☐ | |
| 1.1-2 | 静态辉光 7 种逐项 | ☐ | |
| 1.1-3 | 动画辉光 3 种逐项 | ☐ | |
| 1.1-4 | 强度 0/0.5/2 | ☐ | |
| 1.1-5 | 不使用=无辉光 | ☐ | |
| 1.1-6 | 保存/加载回填 | ☐ | |
| 1.1-7 | 重置恢复默认 | ☐ | |
| 1.2-1 | 生成页辉光选择 | ☐ | |
| 1.2-2 | 在拼装页打开带辉光 | ☐ | |
| 1.3 | CLI --glow / --glow-strength | ☐ | |
| 2.1 | 特效分类删除（自定义+内置） | ☐ | |
| 2.2 | 边框特效分类删除 | ☐ | |
| 3-1~3-10 | 回归 10 项 | ☐ | |
